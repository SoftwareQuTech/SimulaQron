language: python
python:
    - "3.5"
    - "3.6"
    - "3.7"
before_script:
    - export PYTHONPATH=$PYTHONPATH:$(pwd)
install:
    - make clean
    - make python-deps
script:
    - make lint
    - make tests
